[phases.setup]
aptPkgs = ["libcairo2-dev", "pkg-config", "build-essential", "libffi-dev"]

[phases.install]
cmds = ["python -m venv /app/.venv", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
